pipeline {
    agent any

    stages {
        stage('Fetch code') {
            steps {
                echo 'Fetching code'
            }
        }
	 stage('Install webserver') {
            steps {
                echo 'Install webserver'
            }
        }
	 stage('deploy app') {
            steps {
                echo 'deploy app'
            }
        }
    }
}
